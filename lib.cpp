#include "lib.h"
/*
template<typename T, typename A>
bool lib::my_contains(const std::vector<T, std::allocator<A> > &list, const T &item){
    return std::find(list.begin(), list.end(), item) != list.end();
}
*/
